# Dépendances applicatives de previ-R2-D2 (hors torch : installé séparément
# depuis l'index CPU dans le Dockerfile). Résolu par pip au build ;
# `make lock` produit un requirements.lock hashé pour la CI.
#
# rasterio / geopandas / pysheds NE sont PAS ici : lazy-import only dans
# preprocessing/bv/delineation.py, jamais exercés (les 3 centrales ont
# toutes un shapefile connu). Cf. skill hydro-projet.

requests
pandas>=2.0
numpy>=1.26,<2.2
scikit-learn
scipy
pyyaml

lightgbm==4.6.0
optuna==4.6.0
joblib
matplotlib
shap
shapely
pyproj

dvc>=3.5
dvc-http                # remote DVC DagsHub en https

mlflow>=2.16,<3          # tracking + model registry (phase 01)
boto3                    # artefacts MLflow sur MinIO (S3)
psycopg2-binary          # backend store MLflow sur PostgreSQL

pydantic>=2.9
pydantic-settings>=2.6

# API de service (phase 03)
fastapi>=0.115
uvicorn[standard]>=0.30
prometheus-client>=0.20
pyjwt>=2.9                # auth Bearer JWT optionnelle (HS256)
httpx>=0.27              # requis par fastapi.testclient (tests boîte noire)

# Orchestration (phase 05)
prefect>=3.1,<4

# Monitoring (phase 06) — version épinglée sur celle des cours DataScientest.
# Repli KS via scipy (déjà listé) si l'API Evidently bouge.
evidently==0.7.11

ruff
pytest
pytest-cov
